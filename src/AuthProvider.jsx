import React from 'react';
 import {getAuth} from 'firebase/auth';
 
const AuthProvider = () => {
    const auth = getAuth()
    return (
        <div>
            
        </div>
    );
};

export default AuthProvider;